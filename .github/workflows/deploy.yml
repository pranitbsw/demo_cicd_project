name: Demo CI/CD

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4.2.2

      - name: Print Success
        run: echo "CI/CD Pipeline Working Successfully"